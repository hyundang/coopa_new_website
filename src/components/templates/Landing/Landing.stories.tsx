import { Story, Meta } from "@storybook/react";
import Landing from ".";

export default {
  title: "components/templates/Landing",
  component: Landing,
} as Meta;

export const Template: Story = () => <Landing />;
